#include<stdio.h>
int main(){
    char s[30];
    char a[10];
    printf("Enter your name: "); gets(s);
    printf("Your name is %s",s);
    // fflush(stdin);
    // printf("Enter your father's name: ");
    // puts(a);
    // printf("Your father's name is %s",s);

    return 0;
}